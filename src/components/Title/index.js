import React from "react";
import "./style.css";

function Title(props) {
    return <h1 className="title">{props.children}</h1>;
    
}

export default Title;

/// Call the data from JSX.element in ./styles.css to return/////
////title to return a string to the props.children functionality//  